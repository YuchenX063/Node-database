// LIVE PROD server. Selected by `ng build` (production is the default config).
export const environment = {
  name: 'prod',
  production: true,
  apiUrl: 'https://praetor.bc.edu/production/api/',
  exportEnabled: false,
};
